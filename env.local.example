# Neo Lox GmbH ATS - local environment variables
# Copy this file to "env.local" and fill in values.
# IMPORTANT: Never commit env.local (it contains secrets).
#
# Alternatively, you can create a ".env" file with the same keys.

# =============================================================================
# SECRETS - REQUIRED
# =============================================================================
# Generate secure secrets with:
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# WARNING: The application will refuse to start in production mode if these
# are set to insecure dev defaults. Use DevConfig for local development.

SECRET_KEY=dev-secret-change-me
MAGIC_LINK_HMAC_SECRET=dev-hmac-secret

# Optional: separate secret for password reset tokens (defaults to MAGIC_LINK_HMAC_SECRET if empty)
PASSWORD_RESET_HMAC_SECRET=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Data retention (months before cleanup)
RETENTION_MONTHS=6

# Password reset link validity (hours)
PASSWORD_RESET_TTL_HOURS=2

# Public URL (used for links inside emails)
# PRODUCTION: Set this to your actual domain, e.g. https://portal.neo-lox.de
PUBLIC_BASE_URL=http://127.0.0.1:5000

# Privacy policy URL (defaults to https://www.neo-lox.de/datenschutz)
# PRIVACY_URL=https://www.neo-lox.de/datenschutz

# =============================================================================
# EMAIL (Microsoft Graph / Microsoft 365)
# =============================================================================
# Leave empty to use stub logging instead of actual email sending

M365_TENANT_ID=
M365_CLIENT_ID=
M365_CLIENT_SECRET=
M365_SENDER_UPN=hr@neo-lox.de

# Throttle recruiter notification emails for candidate uploads (minutes)
CANDIDATE_UPLOAD_EMAIL_THROTTLE_MINUTES=30

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# For production deployment:
# 1. Generate new secrets (see above)
# 2. Set PUBLIC_BASE_URL to your actual domain
# 3. Configure M365 credentials for email sending
# 4. Use Config class instead of DevConfig in create_app()
# 5. Set FLASK_DEBUG=0 or don't set it at all
# 6. Use a production WSGI server (gunicorn, uWSGI) behind a reverse proxy
